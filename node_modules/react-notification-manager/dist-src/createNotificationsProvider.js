import * as React from "react";
import { Portal } from "reakit/Portal";
import { QueuedNotifications } from "./renderers/QueuedNotifications";
import { useNotificationsState } from "./useNotificationsState";
export const createNotificationsProvider = (NotificationContext) => ({ children, render = QueuedNotifications }) => {
    const state = useNotificationsState();
    return (React.createElement(NotificationContext.Provider, { value: state.actions },
        children,
        React.createElement(Portal, null, React.createElement(render, state))));
};
//# sourceMappingURL=createNotificationsProvider.js.map