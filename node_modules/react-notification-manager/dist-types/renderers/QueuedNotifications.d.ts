/**
 * Picks the notification from the front of the queue and renders it
 */
import * as React from "react";
import { State } from "../NotificationsReducer";
declare type Props = {
    state: State;
};
export declare function QueuedNotifications({ state }: Props): React.FunctionComponentElement<{
    destroy: () => void;
}> | null;
export {};
