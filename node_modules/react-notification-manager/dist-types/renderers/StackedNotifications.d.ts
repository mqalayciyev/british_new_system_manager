/**
 * Shows all notifications in the queue
 */
import * as React from "react";
import { State } from "../NotificationsReducer";
declare type Props = {
    state: State;
};
export declare function StackedNotifications({ state }: Props): React.FunctionComponentElement<{
    key: string;
    destroy: () => void;
}>[];
export {};
